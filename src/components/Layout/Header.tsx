import React from 'react';
import '../../styles/Header.scss';

const Header: React.FC = () => {
  return (
    <header className="header">
      <div className="logo">PORTFOLIO OVERVIEW</div>
    </header>
  );
};

export default Header;
